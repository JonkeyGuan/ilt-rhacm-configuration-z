---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: vault-unsealer
  namespace: acm-subscriptions
spec:
  clusterSelector:
    matchLabels:
      local-cluster: 'true'
